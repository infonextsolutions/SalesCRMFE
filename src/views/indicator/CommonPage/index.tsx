import Navigation from "@/components/app/Navigation";
import React from "react";

const index = ({ title }: any) => {
  return (
    <div>
      <Navigation title={title} buttons={[]} />
    </div>
  );
};

export default index;
